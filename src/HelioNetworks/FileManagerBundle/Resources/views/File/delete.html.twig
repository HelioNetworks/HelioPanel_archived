
    <form method="post" action="{{ url('file_delete') }}" {{ form_enctype(form) }}>

    Are you sure?
        {{ form_widget(form.source, { 'id':'current-file' }) }}

        {{ form_rest(form) }}
    </form>
