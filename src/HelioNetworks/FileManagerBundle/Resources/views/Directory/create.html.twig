
    <form method="post" action="{{ url('file_create') }}" {{ form_enctype(form) }}>
        New directory name:{{ form_widget(form.filename, { 'id':'current-file' }) }}

        {{ form_rest(form) }}
    </form>
